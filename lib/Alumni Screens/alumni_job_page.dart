import 'package:flutter/material.dart';

class AlumniJobPage extends StatelessWidget {
  const AlumniJobPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Alumni Job Page'),
      ),
    );
  }
}
