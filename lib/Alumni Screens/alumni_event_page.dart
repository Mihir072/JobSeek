import 'package:flutter/material.dart';

class AlumniEventPage extends StatelessWidget {
  const AlumniEventPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Alumni Event Page'),
      ),
    );
  }
}
